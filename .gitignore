# .gitignore for a full-stack project with a client/ and server/ structure

# General --------------------------------------------------------------------

# Operating System files
.DS_Store
Thumbs.db

# IDE and editor directories
.vscode/
.idea/

# Dependencies ---------------------------------------------------------------
# The node_modules directory contains all downloaded packages.
# It can be huge and should always be generated by running `npm install`.
# NOTE: You SHOULD commit your lock file (package-lock.json, pnpm-lock.yaml, etc.)
node_modules/

# Build Artifacts ------------------------------------------------------------
# These folders are generated by build tools like Vite or tsc.
dist/
build/
.vite/

# Database -------------------------------------------------------------------
# CRITICAL: Never commit your actual database file to version control.
# It contains sensitive data and changes constantly.
# Instead, commit your Drizzle migration files to recreate the schema.
*.db
*.db-journal
*.db-wal
*.db-shm

# Environment Variables ------------------------------------------------------
# Environment files contain secrets like API keys and database connection strings.
# They should never be committed. Create a .env.example file instead.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test

# Logs & Reports -------------------------------------------------------------
# Log files generated during runtime.
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test and coverage reports.
coverage/
*.lcov

# Temporary Files ------------------------------------------------------------
# Files generated by various tools that don't need to be in version control.
.tmp
.temp